# ------------------------------------------------------------
# Services layer
# ------------------------------------------------------------
# Business logic lives here, decoupled from HTTP route handlers.
# Route handlers should only deal with HTTP concerns (parsing
# request data, returning responses).  All domain logic, data
# aggregation, and third-party API calls belong in services.
#
# Coming next:
#   ai_service.py   — Claude API integration, context assembly,
#                     prompt management, streaming
# ------------------------------------------------------------
